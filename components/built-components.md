## built-components.md format

| design-system name      | file                                                                          |
| ----------------------- | ----------------------------------------------------------------------------- |
| menu-item               | components/MenuItem.tsx                                                       |
| button                  | components/Button.tsx — rebuilt from Figma node 228:7718, structure mirrors design-systems/xops/components/Button.tsx. variant: primary\|secondary\|outline\|link (outline/link: type+class exist, no colors yet — TODO). `icon`/`iconOnly` discriminated union; icon renders as a leading filled badge (was trailing/bare in the old component). Single height scale (`--control-height-large`, 64px) — no size prop yet, small/medium steps TODO. Hover/active driven by shared `--state-*` buckets (on-brand for primary, on-surface for secondary), not per-variant hover tokens |
| nav                     | components/Nav.tsx                                                            |
| case-study-card         | components/CaseStudyCard.tsx                                                  |
| label                   | components/Label.tsx — Clash Display, semibold, uppercase; sizes: xs\|sm\|md\|lg\|xl\|2xl (2xl added for Hero, 32px); color: default(--text-primary)\|inverse(--text-inverse-secondary)\|secondary(--text-secondary)\|tertiary(--text-tertiary)\|accent(--text-accent, added for Hero) |
| title                   | components/Title.tsx — Cabinet Grotesk bold; sizes: xs\|sm\|md\|lg\|xl (xl added for Hero's headline, 96px — Figma source is Extrabold, falls back to Bold, no Extrabold woff2 self-hosted; xl is Title-only, not part of TitleBlock's shared scale); color: default(--text-secondary)\|inverse(--text-inverse-primary)\|tertiary(--text-tertiary) |
| block                   | components/Block.tsx — Cabinet Grotesk regular; sizes: xs\|sm\|md\|lg\|2xl (2xl added for Hero's subline, 32px); color: primary\|secondary(default)\|tertiary\|inverse(--text-inverse-primary). No inline-weight-mixing support — Hero's subline needs mixed regular/bold, temporarily rendered plain regular, deferred |
| label-block             | components/LabelBlock.tsx — Label + optional Block(tertiary); sizes: xs\|sm\|md\|lg\|display; display adds body(statement) + support slots (both Cabinet bold); optional `inverse` prop flips Label + statement/support to the inverse (dark-on-light) tokens, including the word-reveal color pair; optional `labelColor` prop (secondary\|tertiary, sized variants only) overrides the default Label color without affecting Block |
| title-block             | components/TitleBlock.tsx — Title + optional Block(tertiary); sizes: xs\|sm\|md\|lg; optional `inverse` prop flips both to the inverse tokens; optional `titleColor` prop (tertiary) overrides the default Title color without affecting Block |
| card                    | components/Card.tsx — variant: filled\|outline\|ghost; size(xs\|sm\|md\|lg) sets default labelSize+titleSize; labelSize+titleSize as explicit overrides; separator; gap(xs\|sm\|md\|lg\|xl, default md); headerGap(xs\|sm\|md\|lg\|xl, default sm); exposes data-tb-heading on header |
| img-card                | components/ImgCard.tsx — image + caption; single or multi-image; variant: bare(default, no border/padding)\|card(Card outline, old default — all pre-existing call sites pinned to this explicitly); optional `inverse` prop flips caption to --text-inverse-primary for light backgrounds. TODO: migrate remaining `card` call sites to `bare` and remove the variant |
| section                 | components/Section.tsx                                                        |
| hero ⚑NEW               | components/Hero.tsx — home page hero, Figma node 214:7415. Section bg `--surface-section-inverse` (grey-500), min-height 100vh, content bottom-anchored. Label(2xl,accent) + icon badge (`Frame 1321320741.svg`) → Title(xl,inverse, explicit `\n` line breaks via `white-space:pre-line`) + Block(2xl,tertiary, subline — plain regular, bold emphasis deferred) → Button row: primary+icon (`happy.svg`, WORK) + secondary (CONTACT). No rounded corners (Figma's bottom-right-only radius treated as inconsistency, removed) |
| section-introduction    | components/case-studies/software-observability/SectionIntroduction.tsx — case-study hero; Figma-sourced reflow (title+description 1/5, meta row + impact cards stacked 6/12, live `software-experience-embed` moved to full-width row below); meta/impact headings use Title/TitleBlock (Cabinet Grotesk bold, not Label/Clash) per this session's direction to move headings off Clash Display where more legible; Platform Expansion impact item gets a "0 → 1" pill badge (`--surface-card` bg, `--badge-text`, `--radius-pill`). Now a client component with an on-load GSAP entrance timeline (title rows staggered, description together, meta row then impact row in sequence, hero embed fades/translates up in parallel) — hidden-by-default CSS states avoid FOUC. `software-experience-embed`'s ghost-cursor sequence is timed against this timeline's marks. Timing not yet tuned — flagged as too fast |
| the-problem-pinned-scene | components/case-studies/software-observability/TheProblemPinnedScene.tsx — whole Problem section as one sticky-stage scroll sequence: detail block + message board reveal → card grows fullscreen (bg → accent-warning) → messages exit → "The Problem" label → metric cards (real-time count-up) → quote block → stage scrolls away. Absorbs old support-metrics + user-quote sections. Mechanism: `position:sticky` stage inside a `calc(100vh + runway)` track (not GSAP pin). No Figma source |
| data-glossary-table ⚑NEW | components/case-studies/software-observability/DataGlossaryTable.tsx — static 20-row data dictionary (Data Point/Definition/Criticality/Calculation/Pain Points Solved/Source-Integration), fluid-width grid, no props. Structural pattern repurposed from XOPS's Table.tsx (data-driven columns), rebuilt in portfolio tokens — no Figma component-builder pass |
| quote-block             | components/QuoteBlock.tsx — quote + optional emphasis, stacked over optional attribution line. Optional ref-forwarding props (markRef/quoteTextRef/emphasisRef/attributionRef) for external per-piece animation; content/emphasis split via `--quote-content-basis` (default 60%), row gap via `--quote-row-gap`, mark recolor via `--quote-mark-filter` |
| metric-card             | components/MetricCard.tsx — value + label + optional source{label,href} citation link. Optional valueRef (external count-up); hooks: `--metric-source-color` (source vs label), `--metric-label-padding-left` (per-instance label/source shift). Label/source use body-xl(24px) |
| context-block           | components/ContextBlock.tsx                                                   |
| insight-goal-row ⚠orphaned | components/InsightGoalRow.tsx — items: [{label, title, body}, {label, title, body}]; Card(ghost, separator) per item + SVG dashed connector. No longer used anywhere (superseded by `insights-goals-content`'s own card markup in the redesign) — fate undecided, not deleted |
| insights-goals-content  | components/case-studies/software-observability/InsightsGoalsContent.tsx — owns the whole `section.insights-and-goals` scene: inverted-color header (LabelBlock + ContextBlock) + two fixed-height card columns (Insights/Goals), driven by a funnel→spine→badge→connector→extension→card scroll sequence. Funnel is `funnel-morph.svg` morphed via `MorphSVGPlugin` (flat → shape); connectors are `connector-curve.svg` at native size (mirrored, never stretched); spine/badge/connector positions are derived from measured DOM (divider Y, card edges), not hardcoded. No Card reuse — plain heading+divider+body markup, decided during build. See `progress.md` Resume Context for the full mechanism |
| generating-events-content ⚑NEW | components/case-studies/software-observability/GeneratingEventsContent.tsx — owns `section.generating-events`: inverted-color header (LabelBlock display + a body Block) over an image row (2 bare ImgCards) + a step column (3 numbered steps, reusing the `--badge-fill`/`--badge-text` square-badge pattern from `insights-goals-content`). Same `funnel-morph.svg` flat→shape morph as `insights-goals-content`, but the spine has no connectors — it just grows to each step's real measured top edge, fading that step in as it arrives. Funnel/spine x-position is the measured midpoint between the image row and step column, not hardcoded. Figma node `1560:44`, file `C3PsgZV3jZMHgm4bFZJOVP` |
| framework-funnel-spine ⚑NEW | components/case-studies/software-observability/FrameworkFunnelSpine.tsx — one SVG overlay spanning the Framework Adaptation + Observability sections (on a shared `.frameworkObservabilityWrap`): grey-500 funnel morphs flat→shape at Framework top, a spine grows from its peak to a second up-pointing funnel that morphs out of the big eye's almond (base sunk `FUNNEL_ALMOND_SINK`=14px in) and receives the line at full-height tip. Whole line shares the measured almond-center x (one straight vertical); z-layered above the blob / behind the almond via wrapper `isolation`. MorphSVGPlugin + scrubbed ScrollTriggers, ported from `insights-goals-content` (no badges/connectors); almond found via `querySelector('[data-eye="big"]')`. No Figma component-builder pass — blind pass, see `progress.md` Resume Context |
| content-hub             | components/ContentHub.tsx — d3-force hub-and-spoke diagram (title hub + nested leaf/intermediate nodes). Fits the web to its container by scaling node POSITIONS (not text) so it fills a 32px inset — measure-then-fit guarantees no label clips at any size, type stays constant. Spokes stop 16px off each label box; hub type = label-xl, leaves/intermediates = body-lg semibold |
| card-row                | components/CardRow.tsx — horizontal row of Cards; size: fill(default, flex stretch)\|span2(grid-column:span 2 per card, count-independent, requires SectionImg/subgrid ancestor) |
| card-column              | components/CardColumn.tsx — vertical stack of Cards (`<ol>`); width comes from parent track |
| section-img             | components/SectionImg.tsx — image + annotation-card composite Section; layout: row(cards flank image)\|column(cards above/below image)\|corner(full-width CardRow top, image bottom-left, CardColumn bottom-right); props: before?, image (node or array, up to 3), after? |
| quote-marquee ⚑NEW      | components/about/QuoteMarquee.tsx — auto-scrolling testimonial row (CSS loop, pause on hover, reduced-motion fallback); props: quotes[{quote,name,role,href?}], direction: left\|right. Built for /about, no Figma source — pending review |
| number-card ⚑NEW        | components/about/NumberCard.tsx — indexed statement card: display-metric numeral + uppercase label title + body children; hover border/numeral accent. Built for /about, no Figma source — pending review |
| snapshot-gallery ⚑NEW   | components/about/SnapshotGallery.tsx — rotated "snapshot" photo cards with uppercase caption plates, straighten on hover; props: snapshots[{src,alt,caption}]. Built for /about, no Figma source — pending review |
| lazy-mount ⚑NEW         | components/LazyMount.tsx — generic `IntersectionObserver`-gated wrapper (rootMargin, default 600px); mounts children once near the viewport instead of eagerly. `next/dynamic` alone only defers a component's JS download, not its render — this defers the actual mount, for heavy sections that shouldn't compete for main-thread time with whatever's animating above the fold. No Figma source, infra primitive |
| live-embed              | components/LiveEmbed.tsx — scales a live React subtree to fit a container width via ResizeObserver + CSS transform; props: nativeWidth, children, viewportHeight? (pins to a fixed height instead of hugging content height), panTargetIds? (pans the canvas to center matching `[data-hotspot]` elements, scrubbed via panRevealFactor-free direct measurement), scroll? (free-scroll window mode — fills a height-bounded parent and scrolls the scaled canvas natively via a sizer, instead of the transform pan). No Figma source, infra primitive |
| hotspot-overlay         | components/HotspotOverlay.tsx — spotlight mask via a single SVG `<mask>` (one dark fill, one hole per cutout — supports multiple simultaneous targets without their masks compounding), cutout rects smoothed frame-by-frame toward their live target (gsap.ticker + gsap.utils.interpolate, no scale/pulsing-dot), + edge-clamped, corner-aware tooltip card, positioned via measured DOM rects; props: containerRef, active (Hotspot: id/title/label/body/targetIds?/targetSelectors?/subBeats?/placement?), settled. `placement` (top-left/top-right/bottom-left/bottom-right, default top-right) sets which corner of the anchor the tooltip sits at — its sharp corner always points back at the anchor. Targets resolve via `[data-hotspot="id"]` (targetIds, default) or a raw CSS selector scoped to the container (targetSelectors — for elements with no data-hotspot hook available, e.g. a shared component's cells by structural position). No Figma source, built for the hotspot annotation system (`PLAN.md`) |
| overview-prototype-hotspots | components/case-studies/software-observability/OverviewPrototypeHotspots.tsx — wires LiveEmbed + HotspotOverlay + useScrollHotspotSequence around the live OverviewLegacy embed; all 7 hotspots (Over-Assignment skipped), Stage-Level Alerting is a 3-beat hotspot (highlight → modal open → modal close) driving OverviewLegacy's forceAlertsOpen. No Figma source — see `PLAN.md` "Hotspot Annotation System" |
| overview-prototype-2-hotspots ⚑NEW | components/case-studies/software-observability/OverviewPrototype2Hotspots.tsx — same hotspot pattern as overview-prototype-hotspots, embeds the live OverviewScreen (revised, non-legacy) instead; 7 "Decision" hotspots (Over-Assignment skipped). No Figma source |
| all-software-legacy-hotspots ⚑NEW | components/case-studies/software-observability/AllSoftwareLegacyHotspots.tsx — same hotspot pattern, embeds AllSoftwareLegacy; 8 "Assumption" hotspots targeting table columns via HotspotOverlay's new `targetSelectors` (CSS selectors, not `data-hotspot` ids — Table.tsx is a shared XOPS component left untouched). No Figma source |
| software-profile-legacy-hotspots ⚑NEW | components/case-studies/software-observability/SoftwareProfileLegacyHotspots.tsx — same hotspot pattern, embeds SoftwareProfileLegacy (a side-panel body, wrapped in a manually-reproduced SidePanel chrome shell since it's not a freestanding screen); 5 "Assumption" hotspots via real `data-hotspot` ids added to SoftwareProfileLegacy.tsx. Absorbs the old software-profile-quote section. No Figma source |
| software-experience-embed ⚑NEW | components/case-studies/software-observability/SoftwareExperienceEmbed.tsx — the live, navigable "final software experience" for the case-study hero; holds `screen` state and renders the real (non-legacy) XOPS `OverviewScreen`/`AllSoftwareScreen` at nativeWidth=1440 inside LiveEmbed's `scroll` window. Overview⇄All Software swaps in-place via each screen's Sidebar `onNavigate` (not routing, which would leave the portfolio page); All Software→Software Profile is the screen's own SidePanel state. Only the active screen is mounted, wrapped in a `.screenClip` div (own `overflow:hidden` + 16px radius) — AllSoftwareScreen always mounts XOPS's `SidePanel` even closed (just slid off-screen), whose overlay is `position:fixed`; LiveEmbed's `.canvas` transform makes it the containing block, but an ancestor's `overflow:hidden` 2+ levels up (`.wrapper`) doesn't reliably clip it across browsers, so the direct-parent `.screenClip` does instead — scoped to this embed only, no LiveEmbed/SidePanel changes (SidePanel's own always-mounted-fixed-overlay pattern is logged as a deferred architectural item in `xops/PLAN.md`). `.wrapper` itself carries the outer 16px radius + clip (moved here from `SectionIntroduction.module.css`'s `.heroEmbed > *`, which now only sizes it — the component owns its own visual boundary instead of a parent selector reaching in). Owns a 3-phase scripted `ghost-cursor` walkthrough (real dispatched `MouseEvent`s on real DOM targets) — Overview→scroll to Lifecycle table→scroll back→click "All Software" nav→click Adobe Acrobat Pro row (text-matched, confirmed page-1 rank)→hover Inactive Waste/Utilization Rate stats→scroll the Software Profile panel's own internal scroll region (distinct from the embed's outer scroll) to Inactive License Distribution. Timed against `section-introduction`'s entrance animation (cursor fades in at 1.25s, sequence starts at 1.5s) via `waitUntil(t0, ms)`; `primeScroll()` invisibly pre-warms the first scroll target (never-painted content otherwise stalls ~350ms on first paint) while the embed is still fading in. Timing not yet tuned — first-pass values, flagged as too fast. No Figma source for the walkthrough; Phase 4 below is Figma-sourced (node `2:2`, "Portfolio Cleaning" file). **Phase 4 — Layer Inspect reveal (this session, several correction rounds, still not fully visually confirmed):** once the walkthrough finishes AND the embed's top edge scrolls under the nav (`ScrollTrigger`, no pin — both gates independent, whichever lands second fires), the cursor (inlined SVG, was `<img>`, size 64px) parks to the bottom-left (32px/32px) and wiggles (`CustomWiggle` "cursorWiggle" ease) for attention; the embed canvas dims to 75% opacity as 5 message bubbles (exact Figma copy, `TheProblemPinnedScene`'s `.bubble` treatment but 16px/no-wrap text and 8px/12px padding, no speech-tail) mount **progressively** (not pre-mounted+faded — each bubble only enters the DOM on its turn, positioned to the right of the parked cursor, 104px left inset) so the stack genuinely grows from the bottom and pushes earlier bubbles up; the push is a manual measured-rect Y-only glide (capture old `getBoundingClientRect()` before mount, diff against new position after, tween `y: delta → 0` — GSAP Flip was tried and reverted, it also interpolated scale/size and read as bubbles "shuffling" rather than a clean push), ending with an `accent`-variant `Button` ("See how it's built" + inlined `go-arrow` SVG via `currentColor`, was `<img>` — console-log stub, Layer Inspect's stacked-card view itself isn't built yet, see `PLAN.md`). Real interaction stays locked (`.lockOverlay`) through the whole reveal; clicking anywhere on the embed once revealed calls `setLocked(false)` (the "click anywhere to explore" promise in the bubble copy) — the button's own click is separate (console-log only), not wired to unlock. |
| framework-scene ⚑NEW    | components/case-studies/software-observability/FrameworkScene.tsx — pinned spatial diagram (section.framework), ONE pin:true ScrollTrigger + ONE scrubbed timeline. 4 shapes (Insights star, Overview heptagon, All Assets circle, Profiles pentagon) start stacked concentric → Insights settles first → 3 inputs unfurl overlapping-staggered → content rises so heading exits behind nav + row locks under it → connectors draw corner-to-corner into Insights → hold → all 4 wipe to blue-500 (clip-rect reveal) with label/body swapping to new dark content → hold → release. One shared inline SVG, real-geometry resize (not transform:scale) so the shared 1px/6-4 dashed stroke never scales. Figma nodes 1162:6244 (start) / 1188:6589 (framework end) / 1313:3540 (final content). See progress.md Resume Context for full mechanism + tunables |
| observability-eyes ⚑NEW | components/case-studies/software-observability/ObservabilityEyes.tsx — the `section.observability-first` eye collage: 5 light-blue almond eyes (one inlined SVG path scaled) over a blue blob, placed on the 12-col grid (blob + big eye bleed off the right edge). Pupils are CSS circles that follow the cursor via gsap.quickTo (25% of radius, gated to fine-pointer + no-reduced-motion); big eye holds "Observability first." copy revealed by the moving pupil via mix-blend-mode:lighten. Returns a fragment (elements are direct grid children); paths inlined from user SVG exports. No component-builder pass |
| data-scroll-controller ⚑NEW | components/case-studies/software-observability/DataScrollController.tsx — wraps a 100vh `position:sticky` section in a JS-sized tall track and drives an inner `[data-scroll-body]` column's scrollTop from page scroll (1:1, holds `HOLD`=500px at the bottom, then releases). Sticky-stage pattern (not GSAP pin); reduced-motion keeps native scroll. Reused by `section.data` (hubs) and `section.parallel-prototyping` (prototype gallery). Optional `fadeIn` scrubs the scroll column opacity 0→1 over the section's approach (lands with a left-side text reveal). No Figma source |
| gaps-identified-hex-scene ⚑NEW | components/case-studies/software-observability/GapsIdentifiedHexScene.tsx — replaces `section.gaps-identified`'s LabelBlock intro with a freeform 13-hexagon honeycomb (Figma `1472:4207`), sticky 100vh stage (same sticky-stage pattern, not pin); a scrubbed timeline fades hexagons in one-by-one (top → down-left → out-right stagger). Filled hexagons = `--surface-card` div (`clip-path`); 3 outline hexagons = dashed `--surface-card-border` SVG polygon (1px/`6 4`, non-scaling-stroke); 3 hexagons carry the statement text (display-body-sm tier), split across the field. Two hexagons bleed above the stage on purpose (negative Y, no ancestor overflow:hidden). 8px inter-hexagon gap via a shape-only center-scale (text unaffected) |
| lifecycle-timeline-scene ⚑NEW | components/case-studies/software-observability/LifecycleTimelineScene.tsx — replaces `section.lifecycle-timeline`'s static LabelBlock intro with a pinned word-reveal scene (sticky-stage pattern): LabelBlock reveals → hold → 4 words (What/When/Why/Who) fade up staggered → "Who?" description fades in as a trailing beat → hold → releases. No Figma source |
| legacy-experience-embed ⚑NEW | components/case-studies/software-observability/LegacyExperienceEmbed.tsx — the live, fully-connected prototype for `section.testing-the-experience`'s "Full Prototype" ImgCard: real (non-legacy) `OverviewScreen` ⇄ legacy `AllSoftwareLegacy` → `SoftwareProfileLegacy` (its own SidePanel state, unchanged), swapped via Sidebar `onNavigate` inside LiveEmbed's `scroll` window — no hotspots. Fully interactive on load, no ghost-cursor/autoplay/lock (a prior doc entry claimed otherwise — corrected; see `ghost-cursor`, which actually lives on `software-experience-embed` instead). No Figma source |
| ghost-cursor ⚑NEW | components/case-studies/software-observability/GhostCursor.tsx — generic scripted-cursor icon for `software-experience-embed`, `forwardRef`, no built-in positioning/centering logic — the parent owns position/rotation/opacity entirely via GSAP (`x`/`y`/`xPercent`/`yPercent`/`rotation`/`opacity` on the forwarded ref). Icon is inlined SVG (`/SVG/cursor.svg`'s markup directly in JSX, was `<img src>`) — same convention as `MenuItem`'s `go-arrow`. Drop shadow reuses `--xops-elevation-1`'s 5 blur/opacity layers verbatim, redirected to a bottom-left 45° cast via chained `filter: drop-shadow()` — a deliberate one-off XOPS/case-study token crossing, not a new token. No Figma source |
